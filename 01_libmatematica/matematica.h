
/**
 * @param angulo_radianos: valor do ângulo em PI radianos
 * @return Retorna o valor do seno do ângulo dado como parâmetro
 */
float calcular_seno(float angulo_radianos);

/**
 * @param angulo_radianos: valor do ângulo em PI radianos
 * @return Retorna o valor do cosseno do ângulo dado como parâmetro
 */
float calcular_cosseno(float angulo_radianos);


/**
 * @param angulo_radianos: valor do ângulo em PI radianos
 * @return Retorna o valor da tangente do ângulo dado como parâmetro
 */
float calcular_tangente(float angulo_radianos);
